# OGCIO

trigger:
  - dev
  - uat

pr:
  autoCancel: true
  branches:
    include:
      - "*"

parameters:
  - name: buildBranches
    type: object
    default: ["dev", "uat", 'prod']

  - name: validEnvironments
    type: object
    displayName: List of valid environments to deploy (do not change)
    default: ['dev', 'uat', 'prod']

variables:
  - name: pushArtefacts
    value: ${{ containsValue(parameters.buildBranches, variables['Build.SourceBranchName']) }}
  - ${{ if containsValue(parameters.validEnvironments ,variables['Build.SourceBranchName']) }}:
    - template: pipeline-variables/${{ coalesce(variables['Build.SourceBranchName'], 'fallback') }}.yml
  - ${{ else }}:
    - template: pipeline-variables/dev.yml
  - group: ogcio-e2e-test-creds

resources:
  repositories:
    - repository: pipeline-templates
      type: github
      name: ogcio/building-blocks-pipelines
      ref: refs/tags/v0.5.2
      endpoint: ogcio
    - repository: logto-k8s-apps
      type: github
      name: ogcio/logto-k8s-apps
      ref: main
      endpoint: ogcio
    - repository: mygovid-mock-k8s-apps
      type: github
      name: ogcio/mygovid-mock-k8s-apps
      ref: main
      endpoint: ogcio

stages:
  - stage: securityScan
    displayName: Security Scans
    jobs:
      - template: security/gitleaks.yml@pipeline-templates
  - stage: Build_Logto
    displayName: Build Logto
    dependsOn: 
      - securityScan
    jobs:  
    - template: pipeline-templates/build_service.yml
      parameters:
        serviceName: logto
        pushArtefacts: ${{ variables.pushArtefacts }}
        buildArguments: $(buildArguments)
  - stage: Build_MyGovId_Mock
    displayName: Build MyGovId Mock
    dependsOn: 
      - securityScan
    condition: or(eq(variables['Build.SourceBranchName'], 'dev'),eq(variables['Build.Reason'], 'PullRequest'))
    jobs:
    - template: pipeline-templates/build_service.yml
      parameters:
        serviceName: mygovid-mock-service
        pushArtefacts: true
        buildArguments: $(buildArguments)
        dockerfile: ./mygovid-mock-service/Dockerfile
  - stage: Build_Logto_Audit_Cleaner
    displayName: Build Logto Audit Cleaner
    dependsOn: 
      - securityScan
    jobs:
    - template: pipeline-templates/build_service.yml
      parameters:
        serviceName: logto-audit-cleaner
        pushArtefacts: true
        buildArguments: $(buildArguments)
        dockerfile: ./audit-cleaner/Dockerfile
  - stage: Push_Logto_Audit_Cleaner
    displayName: Push Logto Audit Cleaner to ECR
    dependsOn: 
      - EnvApproval
      - Build_Logto_Audit_Cleaner
    condition: succeeded()
    jobs:
    - template: build/push_image_ecr.yml@pipeline-templates
      parameters:
        awsServiceConnection: ${{ variables.awsServiceConnection }}
        awsRegion: ${{ variables.awsRegion }}
        serviceName: logto-audit-cleaner
        repositoryName: bb-logto-audit-cleaner
        pushTag: $(Build.BuildId)
  - stage: OGCIO_E2E_Tests
    displayName: OGCIO E2E Tests
    dependsOn: 
      - Build_Logto
      - Build_Logto_Audit_Cleaner
    condition: succeeded()
    jobs:
      - job: PlaywrightTests
        displayName: Run Playwright E2E Tests
        pool:
          vmImage: 'ubuntu-latest'
        
        variables:
          # Environment variables for the tests
          LOGTO_ADMIN_URL: 'http://localhost:3301'

        steps:
          - checkout: self
            displayName: 'Checkout source code'

          - task: NodeTool@0
            displayName: 'Setup Node.js'
            inputs:
              versionSpec: '20.x'

          - script: |
              npm install -g pnpm@9
            displayName: 'Install pnpm v9'

          - script: |
              npx playwright install chromium
            displayName: 'Install Playwright browsers'
            
          - script: |
              # Use the exact same setup as 'make run-native' for consistency
              echo "🚀 Setting up environment exactly like local 'make run-native'..."
              
              # Step 1: Copy .env file (exactly like makefile)
              echo "Copying .env file..."
              cp .env.sample .env
              echo "✅ .env file copied"
              
              # Step 2: Start PostgreSQL using docker-compose (exactly like makefile)
              echo "Starting database with docker-compose..."
              docker compose -f docker-compose-db.yml up --detach
              echo "✅ Database started"
              
              # Wait for database to be ready with more robust checks
              echo "Waiting for PostgreSQL to be ready..."
              timeout 180 bash -c 'until docker compose -f docker-compose-db.yml exec postgres pg_isready; do echo "Waiting for PostgreSQL..."; sleep 2; done'
              echo "✅ PostgreSQL is accepting connections!"
              
              # Additional check: verify database can actually process queries
              echo "Verifying database is fully operational..."
              timeout 60 bash -c 'until docker compose -f docker-compose-db.yml exec postgres psql -U postgres -d postgres -c "SELECT 1;" > /dev/null 2>&1; do echo "Waiting for database to be fully ready..."; sleep 3; done'
              echo "✅ PostgreSQL is fully operational!"
              
              # Install postgresql-client to test the exact connection Logto will use
              echo "Installing PostgreSQL client for connection testing..."
              sudo apt-get update -qq && sudo apt-get install -y -qq postgresql-client
              
              # Test the exact connection string Logto will use from the host
              echo "Testing exact database connection that Logto will use..."
              timeout 120 bash -c 'until PGPASSWORD=p0stgr3s psql -h localhost -p 5433 -U postgres -d postgres -c "SELECT version();" > /dev/null 2>&1; do echo "Testing Logto connection string..."; sleep 5; done'
              echo "✅ Database connection from host verified!"
              
              # Additional stabilization time specifically for CI environment
              echo "Additional stabilization for CI environment (60 seconds)..."
              sleep 60
              
              # Give database a moment to stabilize
              echo "Allowing database to stabilize..."
              sleep 10
              
              # Check for port conflicts
              echo "Checking port usage:"
              netstat -tlnp | grep :5433 || echo "Port 5433 not found in netstat"
              
              # Ensure the logto database exists using the same connection method Logto will use
              echo "Ensuring logto database exists..."
              PGPASSWORD=p0stgr3s psql -h localhost -p 5433 -U postgres -c "CREATE DATABASE logto;" 2>/dev/null || echo "Database already exists (this is fine)"
              
              # Verify the logto database was created and is accessible
              echo "Verifying logto database is accessible..."
              PGPASSWORD=p0stgr3s psql -h localhost -p 5433 -U postgres -d logto -c "SELECT 'Database logto is accessible';" || echo "Warning: Could not access logto database"
              
              # Test the exact connection string format that Logto uses
              echo "Testing connection string format that Logto will use..."
              timeout 30 bash -c 'until PGPASSWORD=p0stgr3s psql "postgresql://postgres:p0stgr3s@localhost:5433/logto" -c "SELECT 1;" > /dev/null 2>&1; do echo "Testing connection string format..."; sleep 3; done' || echo "Warning: Connection string format test failed"
              
              echo "✅ Logto database ready!"
              
              # Step 3: Install dependencies (exactly like makefile) - MOVED BEFORE seeding
              echo "Installing dependencies..."
              pnpm pnpm:devPreinstall && pnpm i && pnpm prepack
              echo "✅ Dependencies installed"
              
              # Step 4: Seed database (exactly like makefile)
              echo "Seeding database..."
              npm run cli db seed -- --swe && npm run cli db alteration deploy latest && npm run cli db ogcio -- --seeder-filepath="./packages/cli/src/commands/database/ogcio/ogcio-seeder-local.json"
              echo "✅ Database seeded"
              
              # Step 5: Prepare connectors (exactly like makefile)
              echo "Preparing connectors..."
              pnpm connectors build && pnpm cli connector link
              echo "✅ Connectors ready"
              
              # Step 6: Start MyGovId Mock Service (additional for CI)
              echo "Starting MyGovId Mock Service..."
              cd mygovid-mock-service
              npm install
              npm start > ../mygovid-mock.log 2>&1 &
              cd ..
              
              # Wait for MyGovId service
              echo "Waiting for MyGovId Mock Service..."
              timeout 60 bash -c 'until curl -s http://localhost:4005/health > /dev/null; do echo "Waiting for MyGovId..."; sleep 2; done'
              echo "✅ MyGovId Mock Service ready"
              
              # Step 7: Start Logto in dev mode (exactly like makefile)
              echo "Starting Logto in development mode..."
              
              # Debug environment variables and add PostgreSQL timeout settings
              echo "Environment debugging:"
              echo "DB_URL from .env: $(grep DB_URL .env || echo 'Not found')"
              echo "Current working directory: $(pwd)"
              echo "Contents of .env file:"
              cat .env
              
              # Test the actual connection from the same context Logto will use
              echo "Testing database connectivity from the application context..."
              echo "Current PostgreSQL processes:"
              docker compose -f docker-compose-db.yml ps
              echo "Docker networks:"
              docker network ls
              echo "Testing direct connection to database:"
              timeout 10 bash -c 'telnet localhost 5433' || echo "Telnet failed"
              
              # Add comprehensive PostgreSQL client timeout environment variables
              export PGCONNECT_TIMEOUT=120  # Increased from 60
              export PGCOMMAND_TIMEOUT=120  # Increased from 60
              # Additional PostgreSQL connection settings
              export PGOPTIONS='-c statement_timeout=120000'  # 2 minutes
              export PGCLIENTENCODING=UTF8
              
              # Try alternative database URL for CI environment with more generous timeouts
              echo "Original DB_URL: $(grep DB_URL .env)"
              # Update .env to use a more explicit connection string with timeouts and connection parameters
              sed -i 's|DB_URL=postgresql://postgres:p0stgr3s@localhost:5433/logto|DB_URL=postgresql://postgres:p0stgr3s@localhost:5433/logto?connect_timeout=180\&command_timeout=180\&statement_timeout=180000\&idle_in_transaction_session_timeout=300000\&application_name=logto-ci|' .env
              echo "Updated DB_URL: $(grep DB_URL .env)"
              
              echo "Added PostgreSQL timeout environment variables and connection parameters"
              
              pnpm dev > logto-startup.log 2>&1 &
              LOGTO_PID=$!
              
              # Give Logto a moment to start and check for immediate errors
              echo "Checking for immediate startup errors..."
              sleep 45  # Increased significantly to give CI environment more time
              if ! ps -p $LOGTO_PID > /dev/null; then
                echo "❌ Logto died immediately! Checking for database connection issues..."
                echo "Last 50 lines of startup log:"
                tail -50 logto-startup.log
                
                # Check if it's a database connection timeout - if so, try one more time
                if grep -q "timeout expired" logto-startup.log; then
                  echo "🔄 Database timeout detected. Retrying Logto startup (attempt 2)..."
                  sleep 30  # Additional wait time
                  pnpm dev > logto-startup-retry.log 2>&1 &
                  LOGTO_PID=$!
                  sleep 45
                  if ! ps -p $LOGTO_PID > /dev/null; then
                    echo "❌ Logto failed on retry. Retry startup log:"
                    tail -50 logto-startup-retry.log
                    exit 1
                  fi
                  echo "✅ Logto started successfully on retry!"
                  mv logto-startup-retry.log logto-startup.log
                else
                  exit 1
                fi
              fi
              echo "✅ Logto process is running, checking for database connection..."
              
              # Wait for Logto to be ready
              echo "Waiting for Logto to start..."
              RETRY_COUNT=0
              MAX_RETRIES=180  # Increased from 120 to give more time in CI environment
              while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
                if curl -s -f http://localhost:3301/api/status > /dev/null; then
                  echo "✅ Logto is ready!"
                  break
                fi
                RETRY_COUNT=$((RETRY_COUNT + 1))
                echo "Waiting for Logto... (attempt $RETRY_COUNT of $MAX_RETRIES)"
                
                # Check if process is still running
                if ! ps -p $LOGTO_PID > /dev/null; then
                  echo "❌ Logto process died! Last 50 lines of log:"
                  tail -50 logto-startup.log
                  exit 1
                fi
                
                # Show logs every 6 attempts (30 seconds)
                if [ $((RETRY_COUNT % 6)) -eq 0 ]; then
                  echo "📋 Recent Logto logs (at attempt $RETRY_COUNT):"
                  tail -30 logto-startup.log
                fi
                
                sleep 5
              done
              
              if [ $RETRY_COUNT -ge $MAX_RETRIES ]; then
                echo "❌ Logto failed to start. Last 50 lines of log:"
                tail -50 logto-startup.log
                exit 1
              fi
              
              echo "✅ Setup complete - environment matches local 'make run-native'!"
            displayName: 'Setup Environment (Identical to Local)'

          - script: |
              echo "🧪 Final verification..."
              
              # Simple health checks
              echo "Verifying Logto is ready..."
              curl -s -f http://localhost:3301/api/status > /dev/null && echo "✅ Logto healthy" || { echo "❌ Logto unhealthy"; exit 1; }
              
              echo "Verifying MyGovId Mock is ready..."
              curl -s -f http://localhost:4005/health > /dev/null && echo "✅ MyGovId healthy" || { echo "❌ MyGovId unhealthy"; exit 1; }
              
              echo "Verifying database connectivity..."
              docker compose -f docker-compose-db.yml exec postgres pg_isready > /dev/null && echo "✅ Database healthy" || { echo "❌ Database unhealthy"; exit 1; }
              
              echo "✅ All services ready for testing!"
            displayName: 'Final Verification'

          - script: |
              echo "🚀 Running Playwright tests..."
              echo "Tests will create users dynamically via API calls"
              
              # Run tests directly (environment is identical to local now)
              pnpm exec playwright test --project=chromium
              TEST_RESULT=$?
              
              if [ $TEST_RESULT -ne 0 ]; then
                echo "⚠️ Tests failed. Check the test report for details."
                echo "Environment setup was identical to local 'make run-native'"
              fi
              
              exit $TEST_RESULT
            displayName: 'Run Playwright Tests'
            env:
              LOGTO_ADMIN_URL: $(LOGTO_ADMIN_URL)
              TEST_USERNAME: $(TEST_USERNAME)
              TEST_PASSWORD: $(TEST_PASSWORD)
              CI: true
              # Increase browser timeout
              PLAYWRIGHT_BROWSER_TIMEOUT: 180000

          - task: PublishTestResults@2
            displayName: 'Publish Test Results'
            condition: always()
            inputs:
              testResultsFormat: 'JUnit'
              testResultsFiles: 'test-results/results.xml'
              testRunTitle: 'OGCIO E2E Tests - $(environment)'
              mergeTestResults: true

          - task: PublishPipelineArtifact@1
            displayName: 'Upload Playwright Report'
            condition: always()
            inputs:
              targetPath: 'playwright-report'
              artifact: 'playwright-report-$(environment)-$(System.JobId)'
              publishLocation: 'pipeline'

          - task: PublishPipelineArtifact@1
            displayName: 'Upload Test Results'
            condition: always()
            inputs:
              targetPath: 'test-results'
              artifact: 'test-results-$(environment)-$(System.JobId)'
              publishLocation: 'pipeline'
              
          # Collect minimal logs
          - script: |
              echo "📦 Collecting logs..."
              mkdir -p debug-artifacts
              cp logto-startup.log debug-artifacts/ || echo "No logto startup log found"
              cp mygovid-mock.log debug-artifacts/ || echo "No MyGovId mock log found"
            displayName: 'Collect Logs'
            condition: always()
            
          - task: PublishPipelineArtifact@1
            displayName: 'Upload Logs'
            condition: always()
            inputs:
              targetPath: 'debug-artifacts'
              artifact: 'logs-$(environment)-$(System.JobId)'
              publishLocation: 'pipeline'
  - stage: EnvApproval
    displayName: Approvals for deployments - ${{ upper(variables.environment) }}
    dependsOn:
      - Build_Logto
      - OGCIO_E2E_Tests
    condition: ${{ variables.pushArtefacts }}
    jobs:
    - deployment: VerifyDeployment
      displayName: Verify conditions for deployment
      environment: ${{ variables.environment }}
      strategy:
        runOnce:
          deploy:
            steps:
            - script: |
                date
              displayName: Show current date
  - stage: Push_Logto
    displayName: Push logto to ECR
    dependsOn: EnvApproval
    condition: and(${{ variables.pushArtefacts }}, succeeded())
    jobs:
    - template: build/push_image_ecr.yml@pipeline-templates
      parameters:
        awsServiceConnection: ${{ variables.awsServiceConnection }}
        awsRegion: ${{ variables.awsRegion }}
        serviceName: logto
        repositoryName: bb-logto
        pushTag: $(Build.BuildId)
  - stage: Push_MyGovId_Mock
    displayName: Push MyGovId Mock to ECR
    dependsOn: 
      - EnvApproval
      - Build_MyGovId_Mock
    condition: and(eq(variables['Build.SourceBranchName'], 'dev'), succeeded())
    jobs:
    - template: build/push_image_ecr.yml@pipeline-templates
      parameters:
        awsServiceConnection: ${{ variables.awsServiceConnection }}
        awsRegion: ${{ variables.awsRegion }}
        serviceName: mygovid-mock-service
        repositoryName: bb-mygovid-mock
        pushTag: $(Build.BuildId)
  - stage: Deploy_Openshift_MyGovId_Mock
    displayName: GitOps deploy MyGovId Mock
    dependsOn:
      - Push_MyGovId_Mock
    condition: succeeded()
    jobs:
    - template: deploy/gitops.yml@pipeline-templates
      parameters:
        serviceName: mygovid-mock
        clusterName: ${{ variables.clusterName }}
        newName: ${{ variables.ecrEndpoint }}/bb-mygovid-mock
        newTag: $(Build.BuildId)
        environment: dev
  - stage: Deploy_Openshift_Logto
    displayName: GitOps deploy Logto
    dependsOn:
      - EnvApproval
      - Push_Logto
      - Push_Logto_Audit_Cleaner
    condition: succeeded()
    jobs:
      - template: deploy/openshift-gitops.yml@pipeline-templates
        parameters:
          tag: $(Build.BuildId)
          manifestsRepositoryName: logto-k8s-apps
          services:
            - name: logto
              image: ${{ variables.ecrEndpoint }}/bb-logto
              ${{ if containsValue(parameters.validEnvironments ,variables['Build.SourceBranchName']) }}:
                overlaysPath: logto/overlays/${{ variables.clusterName }}/${{ variables['Build.SourceBranchName'] }}
              ${{ else }}:
                overlaysPath: logto/overlays/${{ variables.clusterName }}/dev
            - name: bb-logto-audit-cleaner
              image: ${{ variables.ecrEndpoint }}/bb-logto-audit-cleaner
              ${{ if containsValue(parameters.validEnvironments ,variables['Build.SourceBranchName']) }}:
                overlaysPath: logto/overlays/${{ variables.clusterName }}/${{ variables['Build.SourceBranchName'] }}
              ${{ else }}:
                overlaysPath: logto/overlays/${{ variables.clusterName }}/dev
