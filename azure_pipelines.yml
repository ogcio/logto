# OGCIO

trigger:
  - dev
  - uat

pr:
  autoCancel: true
  branches:
    include:
      - "*"

parameters:
  - name: buildBranches
    type: object
    default: ["dev", "uat", 'prod']

  - name: validEnvironments
    type: object
    displayName: List of valid environments to deploy (do not change)
    default: ['dev', 'uat', 'prod']

variables:
  - name: pushArtefacts
    value: ${{ containsValue(parameters.buildBranches, variables['Build.SourceBranchName']) }}
  - ${{ if containsValue(parameters.validEnvironments ,variables['Build.SourceBranchName']) }}:
    - template: pipeline-variables/${{ coalesce(variables['Build.SourceBranchName'], 'fallback') }}.yml
  - ${{ else }}:
    - template: pipeline-variables/dev.yml
  - group: ogcio-e2e-test-creds

resources:
  repositories:
    - repository: pipeline-templates
      type: github
      name: ogcio/building-blocks-pipelines
      ref: refs/tags/v0.5.2
      endpoint: ogcio
    - repository: logto-k8s-apps
      type: github
      name: ogcio/logto-k8s-apps
      ref: main
      endpoint: ogcio
    - repository: mygovid-mock-k8s-apps
      type: github
      name: ogcio/mygovid-mock-k8s-apps
      ref: main
      endpoint: ogcio

stages:
  - stage: securityScan
    displayName: Security Scans
    jobs:
      - template: security/gitleaks.yml@pipeline-templates
  - stage: Build_Logto
    displayName: Build Logto
    dependsOn: 
      - securityScan
    jobs:  
    - template: pipeline-templates/build_service.yml
      parameters:
        serviceName: logto
        pushArtefacts: ${{ variables.pushArtefacts }}
        buildArguments: $(buildArguments)
  - stage: Build_MyGovId_Mock
    displayName: Build MyGovId Mock
    dependsOn: 
      - securityScan
    condition: or(eq(variables['Build.SourceBranchName'], 'dev'),eq(variables['Build.Reason'], 'PullRequest'))
    jobs:
    - template: pipeline-templates/build_service.yml
      parameters:
        serviceName: mygovid-mock-service
        pushArtefacts: true
        buildArguments: $(buildArguments)
        dockerfile: ./mygovid-mock-service/Dockerfile
  - stage: Build_Logto_Audit_Cleaner
    displayName: Build Logto Audit Cleaner
    dependsOn: 
      - securityScan
    jobs:
    - template: pipeline-templates/build_service.yml
      parameters:
        serviceName: logto-audit-cleaner
        pushArtefacts: true
        buildArguments: $(buildArguments)
        dockerfile: ./audit-cleaner/Dockerfile
  - stage: Push_Logto_Audit_Cleaner
    displayName: Push Logto Audit Cleaner to ECR
    dependsOn: 
      - EnvApproval
      - Build_Logto_Audit_Cleaner
    condition: succeeded()
    jobs:
    - template: build/push_image_ecr.yml@pipeline-templates
      parameters:
        awsServiceConnection: ${{ variables.awsServiceConnection }}
        awsRegion: ${{ variables.awsRegion }}
        serviceName: logto-audit-cleaner
        repositoryName: bb-logto-audit-cleaner
        pushTag: $(Build.BuildId)
  - stage: OGCIO_E2E_Tests
    displayName: OGCIO E2E Tests
    dependsOn: 
      - Build_Logto
      - Build_Logto_Audit_Cleaner
    condition: or(eq(variables['Build.SourceBranchName'], 'dev'), eq(variables['Build.Reason'], 'PullRequest'))
    jobs:
      - job: PlaywrightTests
        displayName: Run Playwright E2E Tests
        pool:
          vmImage: 'ubuntu-latest'
        
        variables:
          # Environment variables for the tests
          LOGTO_ADMIN_URL: 'http://localhost:3302'
          TEST_USERNAME: $(TEST_USERNAME)
          TEST_PASSWORD: $(TEST_PASSWORD)

        steps:
          - checkout: self
            displayName: 'Checkout source code'

          - task: NodeTool@0
            displayName: 'Setup Node.js'
            inputs:
              versionSpec: '20.x'

          - script: |
              npm install -g pnpm@9
            displayName: 'Install pnpm v9'

          - script: |
              pnpm install --frozen-lockfile
            displayName: 'Install dependencies'

          - script: |
              npx playwright install chromium
            displayName: 'Install Playwright browsers'
            
          - script: |
              echo "🔧 Setting up PostgreSQL service..."
              sudo apt-get update
              sudo apt-get install -y postgresql postgresql-contrib
              sudo service postgresql start
              
              # Wait for PostgreSQL to start
              echo "⏳ Waiting for PostgreSQL to start..."
              for i in {1..10}; do
                if sudo -u postgres pg_isready; then
                  echo "✅ PostgreSQL service is running!"
                  break
                fi
                echo "⏳ PostgreSQL not ready yet, waiting... ($i of 10)"
                sleep 2
              done
              
              # Create logto database and user
              echo "Creating logto database and user..."
              sudo -u postgres psql -c "CREATE USER logto WITH PASSWORD 'p0stgr3s';"
              sudo -u postgres psql -c "CREATE DATABASE logto OWNER logto;"
              sudo -u postgres psql -c "GRANT ALL PRIVILEGES ON DATABASE logto TO logto;"
              echo "✅ Database created!"
              
              # Create .env file from sample
              echo "📄 Setting up environment..."
              cp .env.sample .env
              
              # Update .env file with local PostgreSQL settings
              sed -i 's|DB_URL=.*|DB_URL=postgres://logto:p0stgr3s@localhost:5432/logto|g' .env
              
              echo "📄 Environment file created:"
              cat .env
              
              echo "🚀 Running Logto setup..."
              # Pre-run setup steps from makefile but without docker
              pnpm pnpm:devPreinstall && pnpm prepack
              
              echo "🔄 Running database migrations and seeding..."
              npm run cli db seed -- --swe
              npm run cli db alteration deploy latest
              npm run cli db ogcio -- --seeder-filepath="./packages/cli/src/commands/database/ogcio/ogcio-seeder-local.json"
              
              echo "🔌 Building connectors..."
              pnpm connectors build && pnpm cli connector link
              
              echo "🚀 Starting Logto in background..."
              nohup pnpm dev > logto-startup.log 2>&1 &
              
              # Track Logto startup progress
              echo "⏱️ Waiting for Logto to initialize (max 5 minutes)..."
              echo "Started waiting at: $(date)"
              
              # Function to check if server is ready
              check_server() {
                if curl -s http://localhost:3302/ > /dev/null; then
                  return 0  # Success
                else
                  return 1  # Failed
                fi
              }
              
              # Poll for readiness with timeout
              MAX_WAIT=300  # 5 minutes in seconds
              WAIT_INTERVAL=10  # Check every 10 seconds
              ELAPSED=0
              SUCCESS=false
              
              while [ $ELAPSED -lt $MAX_WAIT ]; do
                echo "⏳ Checking if Logto is ready after $ELAPSED seconds..."
                
                if check_server; then
                  echo "✅ Server is responding at http://localhost:3302/ after $ELAPSED seconds"
                  SUCCESS=true
                  break
                fi
                
                # Check for common errors in the logs
                if grep -q "Error: timeout expired" logto-startup.log; then
                  echo "⚠️ Database connection timeout detected!"
                  
                  # Try to fix the database connection issue
                  echo "🔧 Attempting to fix database connection..."
                  cat .env
                  echo "Testing direct database connectivity..."
                  sudo -u postgres psql -c "SELECT 1" || echo "Failed to connect to PostgreSQL"
                  sudo -u postgres psql -c "\l" || echo "Failed to list databases"
                fi
                
                # Check for network issues
                if grep -q "ECONNREFUSED" logto-startup.log; then
                  echo "⚠️ Connection refused error detected!"
                  echo "📊 Network status:"
                  ss -tulpn | grep LISTEN
                  echo "📊 PostgreSQL connectivity check:"
                  nc -zv localhost 5432 || echo "Cannot connect to PostgreSQL port"
                  sudo service postgresql status
                fi
                
                # Print progress and recent logs
                if [ $((ELAPSED % 60)) -eq 0 ]; then
                  echo "📋 Recent log entries (at $ELAPSED seconds):"
                  tail -30 logto-startup.log
                fi
                
                sleep $WAIT_INTERVAL
                ELAPSED=$((ELAPSED + WAIT_INTERVAL))
              done
              
              echo "Finished waiting at: $(date)"
              
              # Final check and detailed reporting
              if $SUCCESS; then
                echo "✅ Logto is running and responding to requests"
              else
                echo "⚠️ WARNING: Logto is not responding properly"
                echo "⚠️ Last 50 lines of logto-startup.log:"
                tail -50 logto-startup.log
                
                echo "📊 PostgreSQL status:"
                sudo service postgresql status
                sudo -u postgres psql -c "\l" || echo "Failed to list databases"
                
                echo "📊 Network status:"
                ss -tulpn | grep LISTEN
                
                echo "📊 Process status:"
                ps aux | grep -E 'node|pnpm|postgres'
                
                echo "📊 Full environment:"
                printenv | grep -v PASSWORD
                
                echo "⚠️ Logto failed to start properly, but we'll attempt tests anyway"
              fi
            displayName: 'Start Database and Logto'

          - script: |
              echo "📝 Pre-test diagnostics:"
              echo "Running tests against: $(LOGTO_ADMIN_URL)"
              echo "Environment: $(environment)"
              echo "Test start time: $(date)"
              echo "Using username: $(TEST_USERNAME)"
              echo "Password provided: $(if [ -n \"$(TEST_PASSWORD)\" ]; then echo 'Yes'; else echo 'No'; fi)"
              
              # One final check of server health
              echo "🔄 Final server connectivity check before running tests..."
              curl -v http://localhost:3302/ || echo "⚠️ Server not responding to final check!"
              
              # Try to get useful info from Logto
              echo "🔄 Checking Logto API health..."
              curl -s http://localhost:3302/.well-known/jwks.json || echo "⚠️ JWKS endpoint not responding"
              
              # Add manual debugging test to check if network is accessible
              echo "🔍 Checking if network is working from container..."
              curl -I https://httpbin.org/status/200
              
              # Run tests with debugging flags and trace enabled
              echo "🚀 Running Playwright tests..."
              PWDEBUG=1 DEBUG=pw:api pnpm exec playwright test --project=chromium --timeout=120000 --retries=1 
              TEST_RESULT=$?
              
              echo "Test end time: $(date)"
              echo "Test exit code: $TEST_RESULT"
              
              # Detailed diagnostics for failures
              if [ $TEST_RESULT -ne 0 ]; then
                echo "⚠️ Tests failed! Running diagnostics..."
                
                echo "📊 Server status:"
                curl -v http://localhost:3302/ || echo "Server not responding"
                
                echo "📋 Logto startup log:"
                cat logto-startup.log
                
                echo "📊 Docker container status:"
                docker ps
                
                echo "📊 Network interfaces:"
                ip addr
                
                echo "📊 Network connectivity:"
                ping -c 4 localhost
                
                echo "📊 Process list:"
                ps aux | grep -E 'node|logto|pnpm'
              fi
              
              exit $TEST_RESULT
            displayName: 'Run Playwright Tests'
            env:
              LOGTO_ADMIN_URL: $(LOGTO_ADMIN_URL)
              TEST_USERNAME: $(TEST_USERNAME)
              TEST_PASSWORD: $(TEST_PASSWORD)
              CI: true
              # Increase browser timeout
              PLAYWRIGHT_BROWSER_TIMEOUT: 180000

          - task: PublishTestResults@2
            displayName: 'Publish Test Results'
            condition: always()
            inputs:
              testResultsFormat: 'JUnit'
              testResultsFiles: 'test-results/results.xml'
              testRunTitle: 'OGCIO E2E Tests - $(environment)'
              mergeTestResults: true

          - task: PublishPipelineArtifact@1
            displayName: 'Upload Playwright Report'
            condition: always()
            inputs:
              targetPath: 'playwright-report'
              artifact: 'playwright-report-$(environment)-$(System.JobId)'
              publishLocation: 'pipeline'

          - task: PublishPipelineArtifact@1
            displayName: 'Upload Test Results'
            condition: always()
            inputs:
              targetPath: 'test-results'
              artifact: 'test-results-$(environment)-$(System.JobId)'
              publishLocation: 'pipeline'
              
          # Collect all logs and debug information
          - script: |
              echo "📦 Collecting debug information..."
              mkdir -p debug-artifacts
              
              # Copy logs
              cp logto-startup.log debug-artifacts/ || echo "No logto startup log found"
              
              # Create system info file
              echo "System information at $(date)" > debug-artifacts/system-info.txt
              echo "======================" >> debug-artifacts/system-info.txt
              echo "Docker status:" >> debug-artifacts/system-info.txt
              docker ps >> debug-artifacts/system-info.txt 2>&1 || echo "Docker not available"
              echo "======================" >> debug-artifacts/system-info.txt
              echo "Network interfaces:" >> debug-artifacts/system-info.txt
              ip addr >> debug-artifacts/system-info.txt 2>&1
              echo "======================" >> debug-artifacts/system-info.txt
              echo "Listening ports:" >> debug-artifacts/system-info.txt
              ss -tulpn >> debug-artifacts/system-info.txt 2>&1
              echo "======================" >> debug-artifacts/system-info.txt
              echo "Environment variables:" >> debug-artifacts/system-info.txt
              printenv | grep -v PASSWORD >> debug-artifacts/system-info.txt
              
              # Collect PostgreSQL logs and status
              echo "Collecting PostgreSQL logs and status..."
              sudo cat /var/log/postgresql/postgresql-*.log > debug-artifacts/postgresql-logs.txt 2>&1 || echo "Could not collect PostgreSQL logs"
              sudo -u postgres psql -c "\l" > debug-artifacts/postgresql-databases.txt 2>&1 || echo "Could not list PostgreSQL databases"
              sudo -u postgres psql -c "\du" >> debug-artifacts/postgresql-databases.txt 2>&1 || echo "Could not list PostgreSQL users"
              
              echo "📦 Debug information collected"
            displayName: 'Collect Debug Information'
            condition: always()
            
          - task: PublishPipelineArtifact@1
            displayName: 'Upload Debug Information'
            condition: always()
            inputs:
              targetPath: 'debug-artifacts'
              artifact: 'debug-info-$(environment)-$(System.JobId)'
              publishLocation: 'pipeline'
  - stage: EnvApproval
    displayName: Approvals for deployments - ${{ upper(variables.environment) }}
    dependsOn:
      - Build_Logto
      - OGCIO_E2E_Tests
    condition: ${{ variables.pushArtefacts }}
    jobs:
    - deployment: VerifyDeployment
      displayName: Verify conditions for deployment
      environment: ${{ variables.environment }}
      strategy:
        runOnce:
          deploy:
            steps:
            - script: |
                date
              displayName: Show current date
  - stage: Push_Logto
    displayName: Push logto to ECR
    dependsOn: EnvApproval
    condition: and(${{ variables.pushArtefacts }}, succeeded())
    jobs:
    - template: build/push_image_ecr.yml@pipeline-templates
      parameters:
        awsServiceConnection: ${{ variables.awsServiceConnection }}
        awsRegion: ${{ variables.awsRegion }}
        serviceName: logto
        repositoryName: bb-logto
        pushTag: $(Build.BuildId)
  - stage: Push_MyGovId_Mock
    displayName: Push MyGovId Mock to ECR
    dependsOn: 
      - EnvApproval
      - Build_MyGovId_Mock
    condition: and(eq(variables['Build.SourceBranchName'], 'dev'), succeeded())
    jobs:
    - template: build/push_image_ecr.yml@pipeline-templates
      parameters:
        awsServiceConnection: ${{ variables.awsServiceConnection }}
        awsRegion: ${{ variables.awsRegion }}
        serviceName: mygovid-mock-service
        repositoryName: bb-mygovid-mock
        pushTag: $(Build.BuildId)
  - stage: Deploy_Openshift_MyGovId_Mock
    displayName: GitOps deploy MyGovId Mock
    dependsOn:
      - Push_MyGovId_Mock
    condition: succeeded()
    jobs:
    - template: deploy/gitops.yml@pipeline-templates
      parameters:
        serviceName: mygovid-mock
        clusterName: ${{ variables.clusterName }}
        newName: ${{ variables.ecrEndpoint }}/bb-mygovid-mock
        newTag: $(Build.BuildId)
        environment: dev
  - stage: Deploy_Openshift_Logto
    displayName: GitOps deploy Logto
    dependsOn:
      - EnvApproval
      - Push_Logto
      - Push_Logto_Audit_Cleaner
    condition: succeeded()
    jobs:
      - template: deploy/openshift-gitops.yml@pipeline-templates
        parameters:
          tag: $(Build.BuildId)
          manifestsRepositoryName: logto-k8s-apps
          services:
            - name: logto
              image: ${{ variables.ecrEndpoint }}/bb-logto
              ${{ if containsValue(parameters.validEnvironments ,variables['Build.SourceBranchName']) }}:
                overlaysPath: logto/overlays/${{ variables.clusterName }}/${{ variables['Build.SourceBranchName'] }}
              ${{ else }}:
                overlaysPath: logto/overlays/${{ variables.clusterName }}/dev
            - name: bb-logto-audit-cleaner
              image: ${{ variables.ecrEndpoint }}/bb-logto-audit-cleaner
              ${{ if containsValue(parameters.validEnvironments ,variables['Build.SourceBranchName']) }}:
                overlaysPath: logto/overlays/${{ variables.clusterName }}/${{ variables['Build.SourceBranchName'] }}
              ${{ else }}:
                overlaysPath: logto/overlays/${{ variables.clusterName }}/dev
